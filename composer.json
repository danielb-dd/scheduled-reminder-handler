{
    "name": "douglassdigital/schedulereminderhandler",
    "description": "CiviCRM extension: scheduled reminder staleness guard, SMTP auto-retry, and non-persistent mailer fix.",
    "license": "AGPL-3.0-or-later",
    "type": "civicrm-extension",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6"
    },
    "scripts": {
        "test": "phpunit"
    },
    "config": {
        "sort-packages": true,
        "platform": {
            "php": "7.4"
        }
    }
}
